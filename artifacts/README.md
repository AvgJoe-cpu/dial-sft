# artifacts/

Runtime-populated directory. Not committed (except scaffolding).

```
artifacts/
├── datasets/       # processed HuggingFace datasets (saved to disk)
├── weights/        # model checkpoints and base model files
└── experiments/    # training logs and tensorboard runs
```
