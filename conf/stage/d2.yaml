# @package _global_
# Stage 2 — D2: refine on best survivors
stage_name: "stage2"
dataset:
  stage: 2
paths:
  train_data: "./artifacts/datasets/base/w_D2"
training:
  num_epochs: 1
eval:
  steps: 1000
  save_strategy: "steps"
